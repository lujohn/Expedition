describe('PlayerService Tests', function () {
	beforeEach(module('expeditionApp'));

	var PlayerService, LandFactory;

	beforeEach(inject(function(_PlayerService_, _LandFactory_) {
		PlayerService = _PlayerService_;
		LandFactory = _LandFactory_;
	}));

	it('should create red settlement', function () {

	});

	it('should create blue road', function () {

	});
});